import React from "react";
import ProfilePage from "../components/ProfilePage";

const Profile = (props) => {
  return (
    <div>
      <ProfilePage {...props} />
    </div>
  );
};

export default Profile;
